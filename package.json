{
  "private": true,
  "scripts": {
    "start": "parcel showcase/index.html",
    "build": "parcel build showcase/index.html --public-url .",
    "build-all": "lerna exec -- npx parcel build src/index.js",
    "publish-all": "yarn build-all && lerna publish",
    "test": "npx standard \"packages/**/*.js\"",
    "clean": "rm yarn.lock && rm -rf node_modules && rm -rf dist && rm -rf .cache",
    "vpush": "git add . && git commit -am 'version update' && git push"
  },
  "devDependencies": {
    "@babel/core": "^7.14.6",
    "babel-eslint": "^10.0.3",
    "emotion": "^10.0.27",
    "lerna": "^4.0.0",
    "np": "^7.2.0",
    "parcel": "^2.0.1",
    "parcel-plugin-svg-sprite": "^1.4.1",
    "standard": "^13.1.0"
  },
  "standard": {
    "parser": "babel-eslint"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead"
}
