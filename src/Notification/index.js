'use strict'
import { Shape, IconText, Direction } from '../'

export default {
  proto: [Shape, IconText],
  caption: {
    icon: 'info',
    text: 'Notification'
  },
  span: 'is not always a distraction'
}